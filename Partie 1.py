class Bond(object):
    def _self_(self, stbond, ltbond):
        self.stbond = stbond
        self.ltbond = ltbond

class bond(object)
    def _self_(self, term, amount, rate):
        self.term = term
        self.amount = amount
        self.rate = rate

